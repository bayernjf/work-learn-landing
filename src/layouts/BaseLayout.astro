---
import SEO from '../components/SEO.astro';
import { getLangFromUrl, type Lang } from '../i18n';
import { SITE_TITLE, SITE_TITLE_EN, SITE_DESCRIPTION, SITE_DESCRIPTION_EN } from '../consts';
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
  lang?: Lang;
  ogImage?: string;
  extraSchema?: object | object[];
}

const { title, description, lang, ogImage, extraSchema } = Astro.props;
const currentLang: Lang = lang ?? getLangFromUrl(Astro.url);

const defaultTitle = currentLang === 'zh' ? SITE_TITLE : SITE_TITLE_EN;
const defaultDesc = currentLang === 'zh' ? SITE_DESCRIPTION : SITE_DESCRIPTION_EN;
const pageTitle = title ?? defaultTitle;
const pageDescription = description ?? defaultDesc;
---

<!doctype html>
<html lang={currentLang === 'zh' ? 'zh-CN' : 'en'}>
  <head>
    <SEO
      title={pageTitle}
      description={pageDescription}
      lang={currentLang}
      pathname={Astro.url.pathname}
      ogImage={ogImage}
      extraSchema={extraSchema}
    />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="theme-color" content="#b8f23e" />
  </head>
  <body class="min-h-screen bg-paper-100 text-ink-900 antialiased">
    <slot />
  </body>
</html>
